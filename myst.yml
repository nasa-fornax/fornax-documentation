# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  title: Fornax User Documentation
  subject: Fornax User Documentation
  # description: Fornax User documentation
  # keywords: []
  github: https://github.com/nasa-fornax/fornax-documentation
  error_rules:
    - rule: link-resolves
      severity: ignore
      keys:
        # some cross linking, ignore these until we can change the upstream source
        - https://docs.fornax.sciencecloud.nasa.gov/user-resource-allotments-and-costs
extends:
  - toc.yml

site:
  template: book-theme
  parts:
     banner: "📢 Announcement: Fornax is in Open Beta."
  options:
     favicon: _static/fornax_favicon.ico
     logo: _static/fornax_logo.png
